"""pluto.compile — ONNX -> quantized .pluto model converter (host, Python).

Phase 0 provides only the model container (format + writer + a demo model).
Later phases add onnx_import, quantize, layout, requant, simulate, and a CLI.
"""
